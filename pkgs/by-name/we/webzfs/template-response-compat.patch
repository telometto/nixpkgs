diff --git a/config/templates.py b/config/templates.py
index 470a7ca..859e5ea 100644
--- a/config/templates.py
+++ b/config/templates.py
@@ -23,7 +23,24 @@ NAV_TABS = [
     Tab(id="fleet", label="Fleet View", url="/fleet/"),
 ]
 
-templates = Jinja2Templates(BASE_DIR / "templates")
+class WebZFSTemplates(Jinja2Templates):
+    def TemplateResponse(self, *args, **kwargs):
+        if args and isinstance(args[0], str):
+            name = args[0]
+            context = args[1] if len(args) > 1 else kwargs.pop("context", None)
+            if context is not None:
+                return super().TemplateResponse(
+                    context["request"],
+                    name,
+                    context,
+                    *args[2:],
+                    **kwargs,
+                )
+
+        return super().TemplateResponse(*args, **kwargs)
+
+
+templates = WebZFSTemplates(BASE_DIR / "templates")
 templates.env.globals["settings"] = settings
 templates.env.globals["NAV_TABS"] = NAV_TABS
 templates.env.globals["get_theme_css_path"] = get_theme_css_path
